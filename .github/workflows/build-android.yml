name: Build Android APK

# Disabled — use PWABuilder.com to generate the signed AAB manually.
# Re-enable by removing the 'if: false' lines once a proper CI solution is set up.
on:
  workflow_dispatch:

jobs:
  build:
    if: false   # disabled until CI build is fixed
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use pwabuilder.com to generate the AAB manually."
